<p class="text-xl text-green-700 pl-6 pt-6"><%= notice %></p>

<div class="pl-6 pt-6">
  <p>
    <strong class="text-xl font-semibold">
      Content:
    </strong>
    <%= @micropost.content %>
  </p>

  <p>
    <strong class="text-xl font-semibold">
      User:
    </strong>
    <%= @micropost.user_id %>
  </p>
</div>

<div class="pl-6 pt-6">
  <%= link_to 'Edit', edit_micropost_path(@micropost), class: "btn-blue" %>
  <%= link_to 'Back', microposts_path, class: "btn-blue" %>
</div>